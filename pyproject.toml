[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "todo-cli"
version = "1.0.0"

[project.scripts]
todo = "todo:main"

[tool.setuptools]
py-modules = ["todo"]
